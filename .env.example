# RealEdge frontend — local environment template (KEY NAMES ONLY, no values).
# Copy to `.env` (gitignored) for local dev. NEVER commit `.env`.
# Production builds MUST set GOOGLE_MAPS_API_KEY as a CI secret instead.
GOOGLE_MAPS_API_KEY=
